; TODO: figure out how to run shit
; this is harder than it seems because Vala doesn't have a standard build system
; meson doesn't have a `meson run` command that we can rely on
; we need to dynamically build tasks from their IDE integration features
